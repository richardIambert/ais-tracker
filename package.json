{
  "name": "ais-tracker-api",
  "version": "1.0.0",
  "description": "WebSocket proxy for AIS Tracker app",
  "scripts": {
    "dev": "nodemon ./src/server.js",
    "start": "node ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
